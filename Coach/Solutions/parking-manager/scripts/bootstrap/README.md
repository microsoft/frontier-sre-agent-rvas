# Bootstrap Scripts

This folder contains bootstrap-only and recovery-only utilities.

Use these scripts when you are preparing a machine or service for the first time, generating certificates, or recovering a component that needs more than the standard workflow rollout.

Do not use these scripts for routine deployments. Normal infrastructure and application deployments are handled by `../../workflows/`.
