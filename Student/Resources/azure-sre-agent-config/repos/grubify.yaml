# Repository that the agent clones for source correlation, issue creation and pull-request
# delivery. It must be the copy the student actually owns, because the student has to review and
# merge the proposed fix, and because the deployed application is built from this same code.
#
# The address is resolved automatically from the `origin` remote of the working copy, so no
# manual configuration is needed. Set GRUBIFY_REPO_URL to override it.
#
# Scope note: the Azure SRE Agent repository resource has no field that restricts the agent to a
# subdirectory, so the whole repository is cloned. Confining the agent to the Grubify folder is a
# behavioural rule expressed in the skill, the subagent prompt and the agent-global instructions,
# not a boundary enforced by the platform.
api_version: azuresre.ai/v1
kind: Repository
metadata:
  name: grubify
spec:
  url: ${GRUBIFY_REPO_URL}
  type: GitHub
  description: Workshop repository containing the Grubify source under Student/Resources/grubify, mounted for incident correlation, code fixes, issue creation and pull-request delivery.
